{
  "name": "paseo-orchestration",
  "private": true,
  "version": "0.2.0",
  "description": "Flows that dispatch Paseo agents: steps with output schemas and declared effects, roles, a run event stream, and a human gate.",
  "type": "module",
  "engines": {
    "node": ">=22.4"
  },
  "exports": {
    "./runtime/*": "./runtime/*",
    "./flows/*": "./flows/*"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "smoke": "node runtime/smoke.mjs"
  }
}
